    function previewFile1() {
        const preview = document.querySelector('.main-img');
        const file = document.querySelector('#main_img').files[0];
        const reader = new FileReader();

        reader.addEventListener("load", function () {
            // convert image file to base64 string
            preview.src = reader.result;
        }, false);

        if (file) {
            reader.readAsDataURL(file);
        }
    }
    function previewFile2() {
        const preview = document.querySelector('.sub-img1');
        const file = document.querySelector('#sub_img1').files[0];
        const reader = new FileReader();

        reader.addEventListener("load", function () {
            // convert image file to base64 string
            preview.src = reader.result;
        }, false);

        if (file) {
            reader.readAsDataURL(file);
        }
    }
    function previewFile3() {
        const preview = document.querySelector('.sub-img2');
        const file = document.querySelector('#sub_img2').files[0];
        const reader = new FileReader();

        reader.addEventListener("load", function () {
            // convert image file to base64 string
            preview.src = reader.result;
        }, false);

        if (file) {
            reader.readAsDataURL(file);
        }
    }
    function previewFile4() {
        const preview = document.querySelector('.sub-img3');
        const file = document.querySelector('#sub_img3').files[0];
        const reader = new FileReader();

        reader.addEventListener("load", function () {
            // convert image file to base64 string
            preview.src = reader.result;
        }, false);

        if (file) {
            reader.readAsDataURL(file);
        }
    }
    function previewFile5() {
        const preview = document.querySelector('.sub-img4');
        const file = document.querySelector('#sub_img4').files[0];
        const reader = new FileReader();

        reader.addEventListener("load", function () {
            // convert image file to base64 string
            preview.src = reader.result;
        }, false);

        if (file) {
            reader.readAsDataURL(file);
        }
    }
    function previewFile6() {
        const preview = document.querySelector('.sub-img5');
        const file = document.querySelector('#sub_img5').files[0];
        const reader = new FileReader();

        reader.addEventListener("load", function () {
            // convert image file to base64 string
            preview.src = reader.result;
        }, false);

        if (file) {
            reader.readAsDataURL(file);
        }
    }
    function previewFile7() {
        const preview = document.querySelector('.sub-img6');
        const file = document.querySelector('#sub_img6').files[0];
        const reader = new FileReader();

        reader.addEventListener("load", function () {
            // convert image file to base64 string
            preview.src = reader.result;
        }, false);

        if (file) {
            reader.readAsDataURL(file);
        }
    }
    function previewFile8() {
        const preview = document.querySelector('.sub-img7');
        const file = document.querySelector('#sub_img7').files[0];
        const reader = new FileReader();

        reader.addEventListener("load", function () {
            // convert image file to base64 string
            preview.src = reader.result;
        }, false);

        if (file) {
            reader.readAsDataURL(file);
        }
    }
    function previewFile9() {
        const preview = document.querySelector('.sub-img8');
        const file = document.querySelector('#sub_img8').files[0];
        const reader = new FileReader();

        reader.addEventListener("load", function () {
            // convert image file to base64 string
            preview.src = reader.result;
        }, false);

        if (file) {
            reader.readAsDataURL(file);
        }
    }
    function previewFile10() {
        const preview = document.querySelector('.sub-img9');
        const file = document.querySelector('#sub_img9').files[0];
        const reader = new FileReader();

        reader.addEventListener("load", function () {
            // convert image file to base64 string
            preview.src = reader.result;
        }, false);

        if (file) {
            reader.readAsDataURL(file);
        }
    }
    function previewFile11() {
        const preview = document.querySelector('.sub-img10');
        const file = document.querySelector('#sub_img10').files[0];
        const reader = new FileReader();

        reader.addEventListener("load", function () {
            // convert image file to base64 string
            preview.src = reader.result;
        }, false);

        if (file) {
            reader.readAsDataURL(file);
        }
    }